[project]
name = "phishing-website-detection"
version = "0.1.0"

dependencies = [
    "flask",
    "gunicorn",
    "requests",
    "beautifulsoup4",
    "numpy",
    "pandas",
    "scikit-learn",
    "scipy",
    "joblib",
    "xgboost"
]

[tool.vercel]
entrypoint = "src.App.app:app"